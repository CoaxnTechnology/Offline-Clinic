# Production Environment Variables
# Copy this to .env.production and fill in values

# Secret key (REQUIRED - generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your_production_secret_key_here_change_this

# Database (PostgreSQL) - Provided by hosting platform
DATABASE_URL=postgresql://user:password@host:5432/database

# Redis (for Celery) - Provided by hosting platform or use Redis Cloud
REDIS_URL=redis://localhost:6379/0

# Flask environment
FLASK_ENV=production

# DICOM Storage Paths (use absolute paths in production)
DICOM_STORAGE_PATH=/app/dicom_files
THUMBNAIL_STORAGE_PATH=/app/thumbnails
PDF_REPORTS_PATH=/app/reports

# DICOM Server Configuration
DICOM_MWL_PORT=11112
DICOM_STORAGE_PORT=11113
DICOM_AE_TITLE=STORESCP

# Logging
LOG_LEVEL=WARNING
LOG_FILE=/app/logs/app.log

# Port (for platforms like Render/Railway)
PORT=5000

# Worker concurrency (for Gunicorn)
WEB_CONCURRENCY=4
